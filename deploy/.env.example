# 生产部署配置模板：复制为 .env 并填入强随机值
# 生成随机串：openssl rand -hex 32
JWT_SECRET=请替换为随机字符串至少32位
INTERNAL_TOKEN=请替换为随机字符串
DB_PASSWORD=请替换为数据库密码
# CORS 允许来源（逗号分隔），生产建议填你的域名
# CORS_ORIGINS=https://pmc.example.com
RATE_LIMIT=120/minute
